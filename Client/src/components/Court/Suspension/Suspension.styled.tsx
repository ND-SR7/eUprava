import styled from "styled-components";

const SuspensionStyled = styled.article`
  background-color: orange;
  border: ${(props) => props.theme.borders.standardBlack};
`;

export default SuspensionStyled;
