import styled from "styled-components";

const WarrantCardStyled = styled.section`
  background-color: red;
  border: ${(props) => props.theme.borders.standardBlack};
`;

export default WarrantCardStyled;
